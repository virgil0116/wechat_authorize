RSpec.describe WechatAuthorize do
  it "has a version number" do
    expect(WechatAuthorize::VERSION).not_to be nil
  end

  it "does something useful" do
    # expect(false).to eq(true)
    expect(true).to eq(true)
  end
end
